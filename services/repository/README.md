# repository

Phase 2 — GitHub connect + webhooks (port **8005**).

- Stub GitHub App install
- Mock or real (`GITHUB_TOKEN`) available repos
- Connect/disconnect repos per project
- `POST /webhooks/github` with HMAC verification → triggers deployment
